repeat task.wait() until game:IsLoaded()

-- ===================== NOTIX INTRO SEQUENCE =====================
do
	local _TS  = game:GetService("TweenService")
	local _SS  = game:GetService("SoundService")
	local _PL  = game:GetService("Players").LocalPlayer
	local _PG  = _PL:WaitForChild("PlayerGui")

	local screenGui = Instance.new("ScreenGui")
	screenGui.Name = "NotixIntro"
	screenGui.ResetOnSpawn = false
	screenGui.IgnoreGuiInset = true
	screenGui.DisplayOrder = 999
	screenGui.Parent = _PG

	local bg = Instance.new("Frame")
	bg.Size = UDim2.new(1,0,1,0)
	bg.BackgroundColor3 = Color3.new(0,0,0)
	bg.BackgroundTransparency = 0.3
	bg.BorderSizePixel = 0
	bg.Parent = screenGui

	local image = Instance.new("ImageLabel")
	image.Size = UDim2.new(1,0,1,0)
	image.BackgroundTransparency = 1
	image.Image = "rbxassetid://9jqQm"
	image.ImageColor3 = Color3.fromRGB(100,200,255)
	image.ImageTransparency = 1
	image.Parent = bg

	local title = Instance.new("TextLabel")
	title.Size = UDim2.new(0.9,0,0.25,0)
	title.Position = UDim2.new(0.05,0,0.35,0)
	title.BackgroundTransparency = 1
	title.Text = "Notix"
	title.TextColor3 = Color3.fromRGB(100,200,255)
	title.TextTransparency = 1
	title.TextScaled = true
	title.Font = Enum.Font.GothamBlack
	title.TextStrokeTransparency = 0.3
	title.TextStrokeColor3 = Color3.new(1,1,1)
	title.Parent = bg

	local subtitle = Instance.new("TextLabel")
	subtitle.Size = UDim2.new(0.9,0,0.1,0)
	subtitle.Position = UDim2.new(0.05,0,0.6,0)
	subtitle.BackgroundTransparency = 1
	subtitle.Text = "Professional Hub"
	subtitle.TextColor3 = Color3.fromRGB(200,220,255)
	subtitle.TextTransparency = 1
	subtitle.TextScaled = true
	subtitle.Font = Enum.Font.Gotham
	subtitle.Parent = bg

	local sound = Instance.new("Sound")
	sound.SoundId = "rbxassetid://7777392893"
	sound.Volume = 0.8
	sound.Looped = false
	sound.Parent = _SS
	sound:Play()

	local fadeIn = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
	_TS:Create(image, fadeIn, {ImageTransparency = 0}):Play()
	_TS:Create(title, fadeIn, {TextTransparency = 0}):Play()
	task.wait(0.1)
	_TS:Create(subtitle, fadeIn, {TextTransparency = 0}):Play()

	task.wait(6)

	local fadeOut = TweenInfo.new(1.2, Enum.EasingStyle.Quad)
	_TS:Create(bg,       fadeOut, {BackgroundTransparency = 1}):Play()
	_TS:Create(image,    fadeOut, {ImageTransparency = 1}):Play()
	_TS:Create(title,    fadeOut, {TextTransparency = 1}):Play()
	_TS:Create(subtitle, fadeOut, {TextTransparency = 1}):Play()

	task.wait(1.3)
	screenGui:Destroy()
	sound:Stop()
	sound:Destroy()
end
-- ===================== END INTRO =====================

local Players    = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS        = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local HttpService  = game:GetService("HttpService")
local Lighting     = game:GetService("Lighting")
local LP = Players.LocalPlayer

-- ===================== CONSTANTS =====================
local NS, CS, LS, DAS, DAD = 60, 30, 12.8, 150, 0.2
local MEDUSA_COOLDOWN      = 25
local STEAL_COOLDOWN       = 0.1
local PLOT_CACHE_DURATION  = 2
local PROMPT_CACHE_REFRESH = 0.15

-- ===================== STATE =====================
local speedMode           = false
local laggerMode          = false
local antiRagdollEnabled  = false
local infJumpEnabled      = false
local medusaCounterEnabled= false
local batCounterEnabled   = false
local unwalkEnabled       = false
local floatEnabled        = false
local floatHeight         = 9.5
local floatJumping        = false
local medusaDebounce      = false
local medusaLastUsed      = 0
local dropActive          = false
local stretchRezEnabled   = false
local autoBatEnabled      = false
local autoLeftEnabled     = false
local autoRightEnabled    = false
local _anyKeyListening    = false
local guiLocked           = true

local KB = {
	DropBrainrot = {kb=Enum.KeyCode.X,           gp=nil},
	AutoLeft     = {kb=Enum.KeyCode.Z,           gp=nil},
	AutoRight    = {kb=Enum.KeyCode.C,           gp=nil},
	AutoBat      = {kb=Enum.KeyCode.E,           gp=nil},
	LaggerMode   = {kb=Enum.KeyCode.R,           gp=nil},
	GuiHide      = {kb=Enum.KeyCode.LeftControl, gp=nil},
	Float        = {kb=Enum.KeyCode.J,           gp=nil},
	SpeedToggle  = {kb=Enum.KeyCode.Q,           gp=nil},
	TPDown       = {kb=Enum.KeyCode.F,           gp=nil},
}
local function kbMatch(e,kc) return kc==e.kb or (e.gp~=nil and kc==e.gp) end

-- ===================== POSITIONS =====================
local AP_L1=Vector3.new(-476.48,-6.28,92.73)
local AP_L2=Vector3.new(-483.12,-4.95,94.80)
local AP_L_FACE=Vector3.new(-482.25,-4.96,92.09)
local AP_R1=Vector3.new(-476.16,-6.52,25.62)
local AP_R2=Vector3.new(-483.06,-5.03,25.48)
local AP_R_FACE=Vector3.new(-482.06,-6.93,35.47)

-- ===================== MODERN COLOR PALETTE =====================
local COLORS = {
	bg_primary    = Color3.fromRGB(15,15,20),
	bg_secondary  = Color3.fromRGB(25,25,32),
	accent_main   = Color3.fromRGB(100,200,255),
	accent_light  = Color3.fromRGB(150,220,255),
	text_primary  = Color3.fromRGB(240,245,250),
	text_secondary= Color3.fromRGB(180,190,200),
	border_main   = Color3.fromRGB(60,80,120),
	border_light  = Color3.fromRGB(50,70,110),
	success       = Color3.fromRGB(100,255,150),
	danger        = Color3.fromRGB(255,100,100),
	hover         = Color3.fromRGB(35,35,45),
}

-- ===== MOBILE PANEL DIMENSIONS =====
local MB_BTN_W  = 72
local MB_BTN_H  = 48
local MB_COLS   = 2
local MB_ROWS   = 4
local MB_PAD    = 4
local MB_CORNER = 8
local MB_TOTAL_W = MB_COLS * MB_BTN_W + (MB_COLS+1) * MB_PAD
local MB_TOTAL_H = MB_ROWS * MB_BTN_H + (MB_ROWS+1) * MB_PAD
local mbPanel   = nil
local Steal={AutoStealEnabled=false,StealRadius=20,StealDuration=0.25,
	Data={},plotCache={},plotCacheTime={},cachedPrompts={},promptCacheTime=0}
local isStealing,stealStartTime,lastStealTick=false,nil,0
local Conns={autoSteal=nil,antiRag=nil,float=nil,anchor={},progress=nil}

-- ===================== FORWARD DECLARED GUI REFS =====================
local progressFill,progressPct,progressRadLbl
local modeValLbl,setFloat
local autoLeftSetVisual,autoRightSetVisual,autoBatSetVisual
local setAutoStealVisual,setInfJumpVisual,setAntiRagVisual
local setMedusaVisual,setUnwalkVisual,setStretchRezVisual,setBatCounterVisual
local setLaggerVisual
local normalBox,carryBox,laggerBox,radInput,durationInput,floatHeightBox
local mbPanelButtons = {}

local function resetProgressBar()
	if progressPct  then progressPct.Text="0%" end
	if progressFill then progressFill.Size=UDim2.new(0,0,1,0) end
end

-- ===================== SAVE CONFIG =====================
local function saveConfig()
	local function ks(e) return {kb=e.kb and e.kb.Name or nil,gp=e.gp and e.gp.Name or nil} end
	local cfg={
		normalSpeed=NS, carrySpeed=CS, laggerSpeed=LS,
		dropBrainrotKey=ks(KB.DropBrainrot), autoLeftKey=ks(KB.AutoLeft),
		autoRightKey=ks(KB.AutoRight), autoBatKey=ks(KB.AutoBat),
		laggerModeKey=ks(KB.LaggerMode), guiHideKey=ks(KB.GuiHide),
		floatKey=ks(KB.Float), speedToggleKey=ks(KB.SpeedToggle),
		tpDownKey=ks(KB.TPDown),
		grabRadius=Steal.StealRadius, stealDuration=Steal.StealDuration,
		antiRagdoll=antiRagdollEnabled, autoStealEnabled=Steal.AutoStealEnabled,
		infiniteJump=infJumpEnabled, medusaCounter=medusaCounterEnabled,
		batCounter=batCounterEnabled, laggerMode=laggerMode,
		carryMode=speedMode, autoBat=autoBatEnabled, unwalkEnabled=unwalkEnabled,
		floatHeight=floatHeight, floatEnabled=floatEnabled, stretchRez=stretchRezEnabled,
		mbBtnW=MB_BTN_W, mbBtnH=MB_BTN_H,
	}
	local btnPositions={}
	for i,btn in ipairs(mbPanelButtons) do
		if btn:IsA("TextButton") then
			local p=btn.Position
			btnPositions[i]={xs=p.X.Scale,xo=p.X.Offset,ys=p.Y.Scale,yo=p.Y.Offset}
		end
	end
	cfg.mbBtnPositions=btnPositions
	if writefile then pcall(function() writefile("NotixConfig.json",HttpService:JSONEncode(cfg)) end) end
end
task.spawn(function() while task.wait(5) do saveConfig() end end)

-- ===================== CREATE MODERN UI =====================
local _PG = LP:WaitForChild("PlayerGui")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "NotixHub"
screenGui.ResetOnSpawn = false
screenGui.IgnoreGuiInset = true
screenGui.Parent = _PG

local UIScale = Instance.new("UIScale", screenGui)
UIScale.Scale = 1

local main = Instance.new("Frame")
main.Name = "MainWindow"
main.Size = UDim2.new(0,650,0,600)
main.Position = UDim2.new(0,40,0,40)
main.BackgroundColor3 = COLORS.bg_primary
main.BorderSizePixel = 0
main.Parent = screenGui

Instance.new("UICorner", main).CornerRadius = UDim.new(0,12)
local mainStroke = Instance.new("UIStroke", main)
mainStroke.Color = COLORS.border_main
mainStroke.Thickness = 1.5

-- ===== HEADER SECTION =====
local header = Instance.new("Frame")
header.Name = "Header"
header.Size = UDim2.new(1,0,0,70)
header.BackgroundColor3 = COLORS.bg_secondary
header.BorderSizePixel = 0
header.Parent = main

Instance.new("UICorner", header).CornerRadius = UDim.new(0,12)

local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Size = UDim2.new(0.7,0,1,0)
titleLabel.Position = UDim2.new(0.05,0,0,0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "NOTIX"
titleLabel.TextColor3 = COLORS.accent_main
titleLabel.TextSize = 32
titleLabel.Font = Enum.Font.GothamBlack
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = header

local subtitleLabel = Instance.new("TextLabel")
subtitleLabel.Name = "Subtitle"
subtitleLabel.Size = UDim2.new(0.7,0,0.4,0)
subtitleLabel.Position = UDim2.new(0.05,0,0.5,0)
subtitleLabel.BackgroundTransparency = 1
subtitleLabel.Text = "Professional Hub v2.2"
subtitleLabel.TextColor3 = COLORS.text_secondary
subtitleLabel.TextSize = 11
subtitleLabel.Font = Enum.Font.Gotham
subtitleLabel.TextXAlignment = Enum.TextXAlignment.Left
subtitleLabel.Parent = header

local statusLight = Instance.new("Frame")
statusLight.Size = UDim2.new(0,12,0,12)
statusLight.Position = UDim2.new(0.88,0,0.35,0)
statusLight.BackgroundColor3 = COLORS.success
statusLight.BorderSizePixel = 0
statusLight.Parent = header

Instance.new("UICorner", statusLight).CornerRadius = UDim.new(1,0)

local function createButton(parent, text, callback, yPos)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0.22,0,0,45)
	btn.Position = UDim2.new(0.04,0,yPos,0)
	btn.BackgroundColor3 = COLORS.bg_secondary
	btn.TextColor3 = COLORS.text_primary
	btn.TextSize = 12
	btn.Font = Enum.Font.GothamBold
	btn.Text = text
	btn.BorderSizePixel = 0
	btn.Parent = parent
	
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)
	local btnStroke = Instance.new("UIStroke", btn)
	btnStroke.Color = COLORS.border_light
	btnStroke.Thickness = 1
	
	btn.MouseEnter:Connect(function()
		TweenService:Create(btn, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {BackgroundColor3 = COLORS.hover}):Play()
	end)
	
	btn.MouseLeave:Connect(function()
		TweenService:Create(btn, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {BackgroundColor3 = COLORS.bg_secondary}):Play()
	end)
	
	btn.MouseButton1Click:Connect(callback)
	return btn
end

-- ===== TAB SECTION =====
local tabContainer = Instance.new("Frame")
tabContainer.Name = "TabContainer"
tabContainer.Size = UDim2.new(1,0,0,50)
tabContainer.Position = UDim2.new(0,0,0,70)
tabContainer.BackgroundColor3 = COLORS.bg_primary
tabContainer.BorderSizePixel = 0
tabContainer.Parent = main

local tabList = Instance.new("Frame")
tabList.Name = "TabList"
tabList.Size = UDim2.new(1,0,1,0)
tabList.BackgroundTransparency = 1
tabList.BorderSizePixel = 0
tabList.Parent = tabContainer

local tabLayout = Instance.new("UIListLayout")
tabLayout.FillDirection = Enum.FillDirection.Horizontal
tabLayout.HorizontalAlignment = Enum.HorizontalAlignment.Left
tabLayout.Padding = UDim.new(0,0)
tabLayout.Parent = tabList

-- ===== CONTENT AREA =====
local contentFrame = Instance.new("Frame")
contentFrame.Name = "ContentFrame"
contentFrame.Size = UDim2.new(1,0,1,-120)
contentFrame.Position = UDim2.new(0,0,0,120)
contentFrame.BackgroundTransparency = 1
contentFrame.BorderSizePixel = 0
contentFrame.Parent = main
contentFrame.ClipsDescendants = true

local contentScroll = Instance.new("ScrollingFrame")
contentScroll.Name = "Scroll"
contentScroll.Size = UDim2.new(1,0,1,0)
contentScroll.BackgroundTransparency = 1
contentScroll.BorderSizePixel = 0
contentScroll.ScrollBarThickness = 5
contentScroll.Parent = contentFrame

local contentScroll_Stroke = Instance.new("UIStroke", contentScroll)
contentScroll_Stroke.Color = COLORS.border_light
contentScroll_Stroke.Thickness = 0.5

local contentLayout = Instance.new("UIListLayout")
contentLayout.Padding = UDim.new(0,15)
contentLayout.SortOrder = Enum.SortOrder.LayoutOrder
contentLayout.Parent = contentScroll

contentScroll.CanvasSize = UDim2.new(0,0,0,0)
contentLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	contentScroll.CanvasSize = UDim2.new(0,0,0,contentLayout.AbsoluteContentSize.Y+30)
end)

-- ===== TABS SETUP =====
local tabs = {}
local currentTab = "Speed"

local function createTab(name)
	local tab = Instance.new("TextButton")
	tab.Name = name
	tab.Size = UDim2.new(0,120,1,0)
	tab.BackgroundColor3 = COLORS.bg_primary
	tab.TextColor3 = COLORS.text_secondary
	tab.TextSize = 13
	tab.Font = Enum.Font.GothamBold
	tab.Text = name
	tab.BorderSizePixel = 0
	tab.Parent = tabList
	
	local tabStroke = Instance.new("UIStroke", tab)
	tabStroke.Color = COLORS.border_light
	tabStroke.Thickness = 1
	
	local underline = Instance.new("Frame")
	underline.Name = "Underline"
	underline.Size = UDim2.new(1,0,0,3)
	underline.Position = UDim2.new(0,0,1,-3)
	underline.BackgroundColor3 = COLORS.accent_main
	underline.BorderSizePixel = 0
	underline.Visible = false
	underline.Parent = tab
	
	tab.MouseButton1Click:Connect(function()
		switchTab(name)
	end)
	
	tabs[name] = {button = tab, underline = underline}
end

local function switchTab(tabName)
	currentTab = tabName
	for name, tab in pairs(tabs) do
		tab.underline.Visible = (name == tabName)
		if name == tabName then
			tab.button.TextColor3 = COLORS.accent_main
		else
			tab.button.TextColor3 = COLORS.text_secondary
		end
	end
	refreshContent()
end

local function createToggle(parent, label, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1,-30,0,40)
	container.Position = UDim2.new(0,15,0,0)
	container.BackgroundColor3 = COLORS.bg_secondary
	container.BorderSizePixel = 0
	container.Parent = parent
	
	Instance.new("UICorner", container).CornerRadius = UDim.new(0,8)
	
	local labelText = Instance.new("TextLabel")
	labelText.Size = UDim2.new(0.7,0,1,0)
	labelText.BackgroundTransparency = 1
	labelText.Text = label
	labelText.TextColor3 = COLORS.text_primary
	labelText.TextSize = 12
	labelText.Font = Enum.Font.GothamBold
	labelText.TextXAlignment = Enum.TextXAlignment.Left
	labelText.Parent = container
	
	local toggle = Instance.new("TextButton")
	toggle.Name = "Toggle"
	toggle.Size = UDim2.new(0,50,0,24)
	toggle.Position = UDim2.new(1,-65,0.5,-12)
	toggle.BackgroundColor3 = COLORS.border_main
	toggle.TextColor3 = Color3.fromRGB(0,0,0)
	toggle.TextSize = 0
	toggle.Text = ""
	toggle.BorderSizePixel = 0
	toggle.Parent = container
	
	Instance.new("UICorner", toggle).CornerRadius = UDim.new(1,0)
	
	local toggleCircle = Instance.new("Frame")
	toggleCircle.Size = UDim2.new(0,20,0,20)
	toggleCircle.Position = UDim2.new(0,2,0.5,-10)
	toggleCircle.BackgroundColor3 = Color3.fromRGB(255,255,255)
	toggleCircle.BorderSizePixel = 0
	toggleCircle.Parent = toggle
	
	Instance.new("UICorner", toggleCircle).CornerRadius = UDim.new(1,0)
	
	local isOn = false
	local function updateToggle(state)
		isOn = state
		if state then
			toggle.BackgroundColor3 = COLORS.success
			TweenService:Create(toggleCircle, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {Position = UDim2.new(0,28,0.5,-10)}):Play()
		else
			toggle.BackgroundColor3 = COLORS.border_main
			TweenService:Create(toggleCircle, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {Position = UDim2.new(0,2,0.5,-10)}):Play()
		end
		if callback then callback(state) end
	end
	
	toggle.MouseButton1Click:Connect(function()
		updateToggle(not isOn)
	end)
	
	return {update = updateToggle, getValue = function() return isOn end}
end

local function createNumberInput(parent, label, defaultValue, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1,-30,0,50)
	container.Position = UDim2.new(0,15,0,0)
	container.BackgroundColor3 = COLORS.bg_secondary
	container.BorderSizePixel = 0
	container.Parent = parent
	
	Instance.new("UICorner", container).CornerRadius = UDim.new(0,8)
	
	local labelText = Instance.new("TextLabel")
	labelText.Size = UDim2.new(0.5,0,0.4,0)
	labelText.Position = UDim2.new(0,15,0,5)
	labelText.BackgroundTransparency = 1
	labelText.Text = label
	labelText.TextColor3 = COLORS.text_primary
	labelText.TextSize = 11
	labelText.Font = Enum.Font.GothamBold
	labelText.TextXAlignment = Enum.TextXAlignment.Left
	labelText.Parent = container
	
	local input = Instance.new("TextBox")
	input.Size = UDim2.new(0,80,0,30)
	input.Position = UDim2.new(1,-95,0.5,-15)
	input.BackgroundColor3 = COLORS.bg_primary
	input.TextColor3 = COLORS.text_primary
	input.TextSize = 12
	input.Font = Enum.Font.Gotham
	input.Text = tostring(defaultValue)
	input.BorderSizePixel = 0
	input.Parent = container
	
	Instance.new("UICorner", input).CornerRadius = UDim.new(0,6)
	local inputStroke = Instance.new("UIStroke", input)
	inputStroke.Color = COLORS.border_light
	inputStroke.Thickness = 1
	
	input.FocusLost:Connect(function()
		local val = tonumber(input.Text)
		if val then
			if callback then callback(val) end
		else
			input.Text = tostring(defaultValue)
		end
	end)
	
	return {input = input, update = function(val) input.Text = tostring(val) end}
end

local function refreshContent()
	for _, child in ipairs(contentScroll:GetChildren()) do
		if child:IsA("GuiObject") and child ~= contentLayout then
			child:Destroy()
		end
	end
	
	if currentTab == "Speed" then
		-- Speed content
		local speedLabel = Instance.new("TextLabel")
		speedLabel.Size = UDim2.new(1,-30,0,30)
		speedLabel.BackgroundTransparency = 1
		speedLabel.Text = "Speed Settings"
		speedLabel.TextColor3 = COLORS.accent_main
		speedLabel.TextSize = 16
		speedLabel.Font = Enum.Font.GothamBlack
		speedLabel.LayoutOrder = 1
		speedLabel.Parent = contentScroll
		
		createNumberInput(contentScroll, "Normal Speed", NS, function(val) NS = math.clamp(val, 10, 200) end).input.LayoutOrder = 2
		createNumberInput(contentScroll, "Carry Speed", CS, function(val) CS = math.clamp(val, 10, 200) end).input.LayoutOrder = 3
		createNumberInput(contentScroll, "Lagger Speed", LS, function(val) LS = math.clamp(val, 1, 100) end).input.LayoutOrder = 4
		
	elseif currentTab == "Features" then
		-- Features content
		local featLabel = Instance.new("TextLabel")
		featLabel.Size = UDim2.new(1,-30,0,30)
		featLabel.BackgroundTransparency = 1
		featLabel.Text = "Feature Toggles"
		featLabel.TextColor3 = COLORS.accent_main
		featLabel.TextSize = 16
		featLabel.Font = Enum.Font.GothamBlack
		featLabel.LayoutOrder = 1
		featLabel.Parent = contentScroll
		
		setAntiRagVisual = function(state)
			-- Update UI visual
		end
		createToggle(contentScroll, "Anti-Ragdoll", function(state)
			antiRagdollEnabled = state
			if state then startAntiRagdoll() else stopAntiRagdoll() end
		end).update(antiRagdollEnabled)
		
		setInfJumpVisual = function(state) end
		createToggle(contentScroll, "Infinite Jump", function(state)
			infJumpEnabled = state
			if state then startInfiniteJump() else stopInfiniteJump() end
		end).update(infJumpEnabled)
		
		setFloat = function(state) end
		createToggle(contentScroll, "Float Mode", function(state)
			floatEnabled = state
			if state then startFloat() else stopFloat() end
		end).update(floatEnabled)
		
	elseif currentTab == "Settings" then
		-- Settings content
		local settLabel = Instance.new("TextLabel")
		settLabel.Size = UDim2.new(1,-30,0,30)
		settLabel.BackgroundTransparency = 1
		settLabel.Text = "Advanced Settings"
		settLabel.TextColor3 = COLORS.accent_main
		settLabel.TextSize = 16
		settLabel.Font = Enum.Font.GothamBlack
		settLabel.LayoutOrder = 1
		settLabel.Parent = contentScroll
		
		createNumberInput(contentScroll, "Steal Radius", Steal.StealRadius, function(val)
			Steal.StealRadius = math.clamp(val, 5, 100)
		end).input.LayoutOrder = 2
		
		createNumberInput(contentScroll, "Float Height", floatHeight, function(val)
			floatHeight = math.clamp(val, 1, 50)
		end).input.LayoutOrder = 3
	end
end

-- Create tabs
createTab("Speed")
createTab("Features")
createTab("Settings")
switchTab("Speed")

-- ===== MINI WINDOW =====
local mini = Instance.new("Frame")
mini.Name = "MiniWindow"
mini.Size = UDim2.new(0,200,0,120)
mini.Position = UDim2.new(0,40,0,680)
mini.BackgroundColor3 = COLORS.bg_secondary
mini.BorderSizePixel = 0
mini.Parent = screenGui

Instance.new("UICorner", mini).CornerRadius = UDim.new(0,10)
local miniStroke = Instance.new("UIStroke", mini)
miniStroke.Color = COLORS.border_main
miniStroke.Thickness = 1.5

local miniTitle = Instance.new("TextLabel")
miniTitle.Size = UDim2.new(1,0,0.3,0)
miniTitle.BackgroundColor3 = COLORS.bg_primary
miniTitle.TextColor3 = COLORS.accent_main
miniTitle.TextSize = 13
miniTitle.Font = Enum.Font.GothamBlack
miniTitle.Text = "Status"
miniTitle.BorderSizePixel = 0
miniTitle.Parent = mini

Instance.new("UICorner", miniTitle).CornerRadius = UDim.new(0,10)

modeValLbl = Instance.new("TextLabel")
modeValLbl.Size = UDim2.new(1,0,0.35,0)
modeValLbl.Position = UDim2.new(0,0,0.3,0)
modeValLbl.BackgroundTransparency = 1
modeValLbl.TextColor3 = COLORS.text_primary
modeValLbl.TextSize = 12
modeValLbl.Font = Enum.Font.Gotham
modeValLbl.Text = speedMode and "Carry Mode" or "Normal Mode"
modeValLbl.Parent = mini

local hideBtn = Instance.new("TextButton")
hideBtn.Size = UDim2.new(1,0,0.35,0)
hideBtn.Position = UDim2.new(0,0,0.65,0)
hideBtn.BackgroundColor3 = COLORS.hover
hideBtn.TextColor3 = COLORS.text_primary
hideBtn.TextSize = 11
hideBtn.Font = Enum.Font.GothamBold
hideBtn.Text = "Hide GUI"
hideBtn.BorderSizePixel = 0
hideBtn.Parent = mini

hideBtn.MouseEnter:Connect(function()
	TweenService:Create(hideBtn, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {BackgroundColor3 = Color3.fromRGB(60,60,75)}):Play()
end)

hideBtn.MouseLeave:Connect(function()
	TweenService:Create(hideBtn, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {BackgroundColor3 = COLORS.hover}):Play()
end)

-- ===== MOBILE BUTTON PANEL =====
mbPanel = Instance.new("Frame")
mbPanel.Name = "MobilePanel"
mbPanel.Size = UDim2.new(0,MB_TOTAL_W,0,MB_TOTAL_H)
mbPanel.Position = UDim2.new(1,-(MB_TOTAL_W+8),0.5,-(MB_TOTAL_H/2))
mbPanel.BackgroundColor3 = COLORS.bg_secondary
mbPanel.BorderSizePixel = 0
mbPanel.Parent = screenGui

Instance.new("UICorner", mbPanel).CornerRadius = UDim.new(0,10)
local mbStroke = Instance.new("UIStroke", mbPanel)
mbStroke.Color = COLORS.border_main
mbStroke.Thickness = 1

local mbLayout = Instance.new("GridLayout")
mbLayout.CellPadding = UDim2.new(0,MB_PAD,0,MB_PAD)
mbLayout.CellSize = UDim2.new(0,MB_BTN_W,0,MB_BTN_H)
mbLayout.Parent = mbPanel

-- Add mobile buttons
local mbBtnConfig = {
	{name="Auto Left", key=KB.AutoLeft},
	{name="Auto Right", key=KB.AutoRight},
	{name="Auto Bat", key=KB.AutoBat},
	{name="Drop", key=KB.DropBrainrot},
	{name="Float", key=KB.Float},
	{name="Speed", key=KB.SpeedToggle},
	{name="Lagger", key=KB.LaggerMode},
	{name="TP Down", key=KB.TPDown},
}

for idx, config in ipairs(mbBtnConfig) do
	local mbBtn = Instance.new("TextButton")
	mbBtn.Name = config.name
	mbBtn.BackgroundColor3 = COLORS.bg_primary
	mbBtn.TextColor3 = COLORS.accent_main
	mbBtn.TextSize = 9
	mbBtn.Font = Enum.Font.GothamBold
	mbBtn.Text = config.name
	mbBtn.BorderSizePixel = 0
	mbBtn.Parent = mbPanel
	
	Instance.new("UICorner", mbBtn).CornerRadius = UDim.new(0,MB_CORNER)
	local mbStroke = Instance.new("UIStroke", mbBtn)
	mbStroke.Color = COLORS.border_light
	mbStroke.Thickness = 0.8
	
	mbBtn.MouseButton1Click:Connect(function()
		UIS:SendKeyEvent(true, config.key.kb, false, nil)
		task.wait(0.05)
		UIS:SendKeyEvent(false, config.key.kb, false, nil)
	end)
	
	mbPanelButtons[idx] = mbBtn
end

local guiVisible = true
local function hideGui()
	guiVisible = false
	TweenService:Create(main, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Position = UDim2.new(-1,0,0,40)}):Play()
	TweenService:Create(mini, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Position = UDim2.new(-1,0,0,680)}):Play()
	TweenService:Create(mbPanel, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Position = UDim2.new(2,0,0.5,-(MB_TOTAL_H/2))}):Play()
end

local function showGui()
	guiVisible = true
	TweenService:Create(main, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Position = UDim2.new(0,40,0,40)}):Play()
	TweenService:Create(mini, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Position = UDim2.new(0,40,0,680)}):Play()
	TweenService:Create(mbPanel, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Position = UDim2.new(1,-(MB_TOTAL_W+8),0.5,-(MB_TOTAL_H/2))}):Play()
end

hideBtn.MouseButton1Click:Connect(function()
	if guiVisible then hideGui() else showGui() end
end)

-- ===== PLACEHOLDER FUNCTIONS =====
local function startAntiRagdoll() end
local function stopAntiRagdoll() end
local function startInfiniteJump() end
local function stopInfiniteJump() end
local function startFloat() end
local function stopFloat() end
local function stopAutoLeft() end
local function stopAutoRight() end
local function startAutoLeft() end
local function startAutoRight() end
local function startLaggerMode() end
local function stopLaggerMode() end
local function runDrop() end
local function runTPDown() end
local function disableAnimations() end
local function enableStretchRez() end
local function startBatCounter() end
local function setupMedusa() end
local function startAutoSteal() end

-- ===================== GLOBAL INPUT HANDLER =====
UIS.InputBegan:Connect(function(inp,gp)
	if gp or _anyKeyListening then return end
	local kc = inp.KeyCode
	local isKeyboard = inp.UserInputType == Enum.UserInputType.Keyboard
	local isGamepad  = inp.UserInputType == Enum.UserInputType.Gamepad1
		or inp.UserInputType == Enum.UserInputType.Gamepad2
		or inp.UserInputType == Enum.UserInputType.Gamepad3
		or inp.UserInputType == Enum.UserInputType.Gamepad4
	if not isKeyboard and not isGamepad then return end
	if kbMatch(KB.SpeedToggle,kc) then
		speedMode=not speedMode
		if modeValLbl then modeValLbl.Text=speedMode and "Carry Mode" or "Normal Mode" end
	elseif kbMatch(KB.LaggerMode,kc) then
		laggerMode=not laggerMode
		if setLaggerVisual then setLaggerVisual(laggerMode) end
		if laggerMode then startLaggerMode() else stopLaggerMode() end
	elseif kbMatch(KB.AutoBat,kc) then
		autoBatEnabled=not autoBatEnabled
		if autoBatEnabled then
			if autoLeftEnabled then autoLeftEnabled=false;if autoLeftSetVisual then autoLeftSetVisual(false) end;stopAutoLeft() end
			if autoRightEnabled then autoRightEnabled=false;if autoRightSetVisual then autoRightSetVisual(false) end;stopAutoRight() end
		end
	elseif kbMatch(KB.AutoLeft,kc) then
		autoLeftEnabled=not autoLeftEnabled
		if autoLeftEnabled then
			if autoRightEnabled then autoRightEnabled=false;if autoRightSetVisual then autoRightSetVisual(false) end;stopAutoRight() end
			if autoBatEnabled then autoBatEnabled=false end
			startAutoLeft()
		else
			stopAutoLeft()
		end
	elseif kbMatch(KB.AutoRight,kc) then
		autoRightEnabled=not autoRightEnabled
		if autoRightEnabled then
			if autoLeftEnabled then autoLeftEnabled=false;if autoLeftSetVisual then autoLeftSetVisual(false) end;stopAutoLeft() end
			if autoBatEnabled then autoBatEnabled=false end
			startAutoRight()
		else
			stopAutoRight()
		end
	elseif kbMatch(KB.DropBrainrot,kc) then
		task.spawn(runDrop)
	elseif kbMatch(KB.TPDown,kc) then
		task.spawn(runTPDown)
	elseif kbMatch(KB.Float,kc) then
		floatEnabled=not floatEnabled
		if setFloat then setFloat(floatEnabled) end
		if floatEnabled then floatJumping=false;startFloat() else stopFloat() end
	elseif kbMatch(KB.GuiHide,kc) then
		if guiVisible then hideGui() else showGui() end
	end
end)

-- ===== LOAD CONFIG =====
local function loadConfig()
	if not(isfile and isfile("NotixConfig.json")) then return end
	local ok,cfg=pcall(function() return HttpService:JSONDecode(readfile("NotixConfig.json")) end)
	if not ok or not cfg then return end
	local function lk(entry,data)
		if type(data)~="table" then return end
		if data.kb and Enum.KeyCode[data.kb] then entry.kb=Enum.KeyCode[data.kb] end
		if data.gp and Enum.KeyCode[data.gp] then entry.gp=Enum.KeyCode[data.gp] end
	end
	if cfg.normalSpeed then NS=cfg.normalSpeed end
	if cfg.carrySpeed then CS=cfg.carrySpeed end
	if cfg.laggerSpeed then LS=cfg.laggerSpeed end
	if cfg.grabRadius then Steal.StealRadius=cfg.grabRadius end
	if cfg.stealDuration then Steal.StealDuration=cfg.stealDuration end
	if cfg.floatHeight then floatHeight=cfg.floatHeight end
	lk(KB.DropBrainrot,cfg.dropBrainrotKey);lk(KB.AutoLeft,cfg.autoLeftKey)
	lk(KB.AutoRight,cfg.autoRightKey);lk(KB.AutoBat,cfg.autoBatKey)
	lk(KB.LaggerMode,cfg.laggerModeKey);lk(KB.GuiHide,cfg.guiHideKey)
	lk(KB.Float,cfg.floatKey);lk(KB.SpeedToggle,cfg.speedToggleKey)
	lk(KB.TPDown,cfg.tpDownKey)
	if cfg.antiRagdoll then antiRagdollEnabled=true;if setAntiRagVisual then setAntiRagVisual(true) end;startAntiRagdoll() end
	if cfg.autoStealEnabled then Steal.AutoStealEnabled=true;if setAutoStealVisual then setAutoStealVisual(true) end;pcall(startAutoSteal) end
	if cfg.infiniteJump then infJumpEnabled=true;if setInfJumpVisual then setInfJumpVisual(true) end;startInfiniteJump() end
	if cfg.laggerMode then laggerMode=true;if setLaggerVisual then setLaggerVisual(true) end;startLaggerMode() end
	if cfg.carryMode then speedMode=true;if modeValLbl then modeValLbl.Text="Carry Mode" end end
	if cfg.floatEnabled then floatEnabled=true;if setFloat then setFloat(true) end;floatJumping=false;startFloat() end
end

switchTab("Speed")
loadConfig()
print("[Notix v2.2] Loaded Successfully - Professional UI Active!")
